import { HeroSection } from '@/components/blocks/hero-section'; // Import the new HeroSection

export const HomePage = () => {
  return (
    // Render the HeroSection directly
    // The HeroSection includes its own header and main content structure
    <HeroSection />
  );
};