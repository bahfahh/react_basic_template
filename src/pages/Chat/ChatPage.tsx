import { ChatContainer } from '../../components/chat/ChatContainer/ChatContainer';

export const ChatPage = () => {
  return (
    <div className="h-full"> {/* Use h-full to fill parent container */}
      <ChatContainer />
    </div>
  );
};